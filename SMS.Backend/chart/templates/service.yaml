apiVersion: v1
kind: Service
metadata:
  name: {{ include "sms-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sms-backend.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "sms-backend.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
