apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sms-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sms-backend.labels" . | nindent 4 }}
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.databaseUrl | quote }}
