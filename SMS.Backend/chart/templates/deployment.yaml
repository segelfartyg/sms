apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sms-backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sms-backend.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "sms-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "sms-backend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "sms-backend.fullname" . }}
                  key: DATABASE_URL
            - name: WAREHOUSE_URL
              value: {{ .Values.warehouseUrl | quote }}
            {{- if .Values.allowedOrigins }}
            - name: ALLOWED_ORIGINS
              value: {{ .Values.allowedOrigins | quote }}
            {{- end }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
